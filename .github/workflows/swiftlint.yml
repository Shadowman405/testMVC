name: SwiftLint

on:
  pull_request:
    branches:
      - main

jobs:
  SwiftLint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run SwiftLint
        run: |
          swiftlint lint --strict --quiet
